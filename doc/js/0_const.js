function wow() {
  if (true) {
    if (true) {
      var myVar = "hello";
      console.log(myVar);
    }
  }
}

wow();

// var is bad and reachable through the scope function.
// let allows to reasign a value inside the same scope
// const blocks re-assignment.
